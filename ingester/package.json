{
  "name": "aquarium-control-ingester",
  "version": "3.0.0",
  "description": "The ingester that receives data from controllers, part of the aquarium control system",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "tslint src/**/*.ts",
    "build": "rimraf dist && tsc",
    "postbuild": "echo done",
    "start": "./bin/ingester.js"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/nebrius/aquarium-control.git"
  },
  "author": "Bryan Hughes <bryan@nebri.us>",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/nebrius/aquarium-control/issues"
  },
  "homepage": "https://github.com/nebrius/aquarium-control#readme",
  "devDependencies": {
    "@types/bluebird": "^3.5.11",
    "@types/node": "^8.0.28",
    "rimraf": "^2.6.2",
    "tslint": "^5.7.0",
    "typescript": "^2.5.2"
  },
  "dependencies": {
    "azure-event-hubs": "0.0.8"
  }
}
